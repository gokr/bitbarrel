import 'package:flutter/material.dart';

class BarrelExplorerScreen extends StatelessWidget {
  const BarrelExplorerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Barrel Explorer'),
      ),
      body: const Center(
        child: Text('Barrel Explorer - Coming Soon'),
      ),
    );
  }
}

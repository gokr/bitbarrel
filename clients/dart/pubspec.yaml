name: bitbarrel
description: Dart/Flutter client library for BitBarrel key-value store with WebSocket protocol
version: 1.0.0
homepage: https://github.com/tankfeed/bitbarrel

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  web_socket_channel: ^2.4.0
  mutex: ^3.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
